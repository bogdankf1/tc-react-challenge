export const getNewReleases = (state) => state.musicData.newReleases;

export const getPlaylists = (state) => state.musicData.playlists;

export const getCategories = (state) => state.musicData.categories;

export const getError = (state) => state.musicData.error;

export const getLoading = (state) => state.musicData.loading;
